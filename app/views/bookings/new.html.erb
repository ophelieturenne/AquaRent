<div class = "container mb-5">
    <div class="booking-container">
      <div class="booking-content-left bg-white shadow justify-content-center" >
        <h4 class="mb-3">Creating a new booking for <strong><%= @material.name %></strong></h4>
        <%if @material.photo.attached? %>
          <%= cl_image_tag @material.photo.key, :crop => "fit" %>
        <%else%>
          <img src="https://res.cloudinary.com/dvp1v2dej/image/upload/v1665598402/aquarent/water_tzufvy.jpg" />
        <%end %>
      </div>

      <div class="booking-content-right bg-white shadow" >
        <%= simple_form_for ([@material, @booking]) do |f| %>
        <%= f.input :start_date, as: :date, html5: true%>
        <%= f.input :end_date, as: :date, html5: true%>
        <%= f.submit class: 'btn btn-primary' %>
        <% end %>
      </div>
    </div>
</div>
